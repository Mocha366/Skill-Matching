import { db } from "../../../firebase";
import { collection, addDoc, query, where, getDocs } from "firebase/firestore";

export const likeUser = async (fromUserId: string, toUserId: string, toUserName: string) => {
    try {
        const
    }
}